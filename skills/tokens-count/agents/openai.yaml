interface:
  display_name: "Codex Usage Count"
  short_description: "Count Codex tokens, turns, requests, and tools"
  default_prompt: "Use $tokens-count to report tokens, turns, requests, tool calls, and threads for selected Codex tasks."

policy:
  allow_implicit_invocation: false
