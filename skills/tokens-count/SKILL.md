---
name: tokens-count
description: Count local Codex tokens and Cursor activity by thread or time range.
---

# Count thread usage

When the CLI syntax is unknown or may have changed, consult its authoritative reference:

```text
python <skill-dir>/scripts/count_tokens.py --help
```

Use that output, rather than this skill, for current arguments, defaults, examples, and output modes. Do not repeat the help call when the required syntax is already known.

Install the pinned TOON dependency before first use:

```text
python -m pip install -r <skill-dir>/requirements.txt
```

Read [implementation.md](implementation.md) only when debugging behavior or modifying the script.
